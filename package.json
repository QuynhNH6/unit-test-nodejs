{
  "name": "unit-test",
  "version": "1.0.0",
  "scripts": {
    "test": "NODE_ENV=test mocha --exit --timeout 1000000",
    "test:report": "NODE_ENV=test nyc --reporter=html --reporter=text mocha --exit --timeout 1000000"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-fuzzy": "^1.6.1",
    "chai-http": "^4.3.0",
    "mocha": "^6.2.2",
    "nyc": "^15.0.0"
  }
}
